.vscode/
.env
models/*
runs/
output*/

build/

**/__pycache__
*.pyc

# Finetuning
finetuning/logs/
finetuning/phi3-*
finetuning/raw_examples.txt

mesh_pipeline/data/*
mesh_pipeline/.vscode/*
habitat-sim/semantic_captures/*
mesh_pipeline/outputs/*

# Saved navigation frame captures (runtime output from the viewer's save-frames)
**/saved_captures/

# Vendored third-party clones (cloned on-demand; not our source to commit)
mesh_pipeline/third_party/MaskClustering/

# Top-level runtime data: NavVis sessions, built HGE semantic outputs,
# hloc feature caches. Large (GB-scale) and user-specific.
data/

docs/
CLAUDE.md

# Build + local artifacts
cnsg.egg-info/
.pytest_cache/
.ruff_cache/

# Vendored hloc (fetched with --recursive by scripts/install.sh)
third_party/

# Claude Code local state
.claude/